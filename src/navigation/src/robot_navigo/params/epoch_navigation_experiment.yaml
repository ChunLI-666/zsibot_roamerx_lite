# Load after base, forward_alignment_experiment and model footprint overlays.
# This is an opt-in experiment. Motion recovery remains a separate work item;
# strict BT performs only clearing and waiting, never untagged Spin/BackUp.
bt_navigator:
  ros__parameters:
    enable_epoch_contract: true
controller_server:
  ros__parameters:
    enable_epoch_contract: true
velocity_smoother:
  ros__parameters:
    enable_epoch_contract: true
nav_safety_gate:
  ros__parameters:
    enable_epoch_contract: true
